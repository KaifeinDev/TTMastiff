import 'package:flutter/material.dart';

class BookingManageScreen extends StatelessWidget {
  const BookingManageScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('預約審核功能開發中...'));
  }
}
